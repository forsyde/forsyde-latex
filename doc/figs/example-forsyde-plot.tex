\documentclass{standalone}
\usepackage[plot]{forsyde}

\def\sinker{2 * pi * 7.14286}
\def\sigfM(#1){sin(2 * pi * 7.14286 * deg(#1-.07))}
\def\sigfP(#1){sin(2 * pi * 7.14286 * deg(#1+.07))}

\def\sigsin(#1){sin(2 * pi * 7.14286 * deg(#1-.07))}
% \def\sigsinI(#1){\sigfP(#1) + 1}
% \def\sigsinII(#1){\sigfP(#1) + \sigfP(#1) + 1}

% \def\osinI(#1){\sigfP(#1)}
% \def\osinII(#1){\sigfP(#1) + \sigfP(#1)}
% \def\osinIII(#1){\sigfP(#1) + \sigfP(#1) + 1}
% \def\osinIV(#1){\sigfP(#1) + \sigfP(#1) + 1 + 1}

\begin{document}
\begin{ct-signal}[xmin=0, xmax=.35, ymin=-1.5, ymax=1.5, every axis plot/.append style={ultra thick}]{1.3}
  \ctsignal{}\ctct{0}{.4}{1}\ctend
  \ctsignal{}\ctf{0}{.4}{\sigsin}\ctend
  \ctsignal{}\ctct{0}{.14}{0}\ctct{.14}{.28}{1}\ctct{.28}{.4}{0}\ctend
  \ctsignal{}\ctct{0}{.4}{-1}\ctend
\end{ct-signal} 
\end{document}

%%% Local Variables:
%%% mode: latex
%%% TeX-master: "../date2016"
%%% End:
